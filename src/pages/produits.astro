---
import Layout from '../layouts/Layout.astro';

const allInOne = [
  {
    name: 'Serie M',
    type: 'Colonne All-in-One',
    pitch: '0.9 - 2.5mm',
    description: "Mur LED d'entree de gamme avec fonctions PIP integrees. Solution ideale pour les salles de reunion et espaces collaboratifs.",
    features: ['PIP integre', 'Installation en colonne', 'Plug & Play', 'Entree de gamme'],
    tag: 'Populaire',
    image: 'https://images.unsplash.com/photo-1542744173-8e7e53415bb0?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'Serie URL',
    type: 'Colonne All-in-One',
    pitch: '0.9 - 1.8mm',
    description: "Mur LED tactile premium avec processeur PIP haut de gamme. L'experience interactive ultime pour les espaces de decision.",
    features: ['Ecran tactile', 'Processeur PIP premium', 'Interactif', 'Ultra HD'],
    tag: 'Premium',
    image: 'https://images.unsplash.com/photo-1497215842964-222b430dc094?w=400&h=280&fit=crop&q=80',
  },
];

const cabinets = [
  {
    name: 'VDS',
    type: 'Cabinet LED - COB',
    pitch: '0.4 - 1.2mm',
    description: "Technologie COB de pointe avec option cadre tactile. Performance et durabilite exceptionnelles pour les installations permanentes.",
    features: ['Technologie COB', 'Cadre tactile optionnel', 'Ultra haute resolution', 'Maintenance facilitee'],
    tag: 'Innovation',
    image: 'https://images.unsplash.com/photo-1550751827-4bd374c3f58b?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'VHC',
    type: 'Cabinet LED - SMD',
    pitch: '1.2 - 2.5mm',
    description: "Solution SMD entree de gamme pour les projets a budget maitrise sans compromis sur la qualite.",
    features: ['SMD entree de gamme', 'Budget maitrise', 'Fiabilite', 'Grand format'],
    image: 'https://images.unsplash.com/photo-1504384308090-c894fdcc538d?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'MGP',
    type: 'Cabinet LED - SMD',
    pitch: '0.9 - 1.8mm',
    description: "Gamme SMD polyvalente offrant un excellent compromis entre performance et accessibilite.",
    features: ['SMD polyvalent', 'Rapport qualite/prix', 'Modulaire', 'Multi-usage'],
    image: 'https://images.unsplash.com/photo-1497366811353-6870744d04b2?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'TVF',
    type: 'Cabinet LED - SMD',
    pitch: '0.7 - 1.5mm',
    description: "SMD milieu de gamme avec une qualite d'image remarquable. Le choix de reference pour les projets professionnels.",
    features: ['Milieu de gamme', 'Image remarquable', 'Professionnel', 'Polyvalent'],
    tag: 'Best-seller',
    image: 'https://images.unsplash.com/photo-1531973576160-7125cd663d86?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'DLP',
    type: 'Cabinet LED - SMD Premium',
    pitch: '0.7 - 1.2mm',
    description: "SMD haut de gamme fabrique en Union Europeenne. La reference premium pour les installations les plus exigeantes.",
    features: ['Fabrique en UE', 'Haut de gamme', 'Certifie', 'Support premium'],
    tag: 'Made in EU',
    image: 'https://images.unsplash.com/photo-1518770660439-4636190af475?w=400&h=280&fit=crop&q=80',
  },
];

const specialty = [
  {
    name: 'LUW',
    type: 'Outdoor',
    pitch: '2.5 - 6mm',
    description: "Pitch ultra-fin pour l'exterieur avec haute luminosite. Images eclatantes meme en plein soleil.",
    features: ['Exterieur', 'Ultra-fin outdoor', 'Haute luminosite', 'IP65+'],
    tag: 'Outdoor',
    image: 'https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=400&h=280&fit=crop&q=80',
  },
  {
    name: 'UC',
    type: 'Formats speciaux',
    pitch: '0.9 - 1.5mm',
    description: "Formats 21/9 et 32/9 pour des installations cinematiques et immersives uniques.",
    features: ['Format 21/9', 'Format 32/9', 'Cinematique', 'Immersif'],
    tag: 'Special',
    image: 'https://images.unsplash.com/photo-1478720568477-152d9b164e26?w=400&h=280&fit=crop&q=80',
  },
];

function renderProducts(products: typeof allInOne) {
  return products;
}
---

<Layout title="Nos produits LED" description="Decouvrez notre gamme complete d'ecrans LED : colonnes all-in-one, cabinets SMD et COB, solutions outdoor.">

  <section class="page-hero">
    <div class="container">
      <span class="section-label">Catalogue complet</span>
      <h1>Nos ecrans LED</h1>
      <p class="page-hero-desc">
        Des packages tout-en-un incluant mecanique, cabinets, cables,
        electronique, finitions et pieces de rechange.
      </p>
    </div>
  </section>

  <!-- All-in-One -->
  <section class="section" id="all-in-one">
    <div class="container">
      <div class="category-header">
        <h2>Colonnes All-in-One</h2>
        <p>Solutions integrees en colonne avec processeur LED embarque. Branchez, allumez, projetez.</p>
      </div>
      <div class="products-grid">
        {allInOne.map(product => (
          <div class="product-card">
            <div class="card-img">
              <img src={product.image} alt={product.name} loading="lazy" />
              {product.tag && <span class="card-tag">{product.tag}</span>}
            </div>
            <div class="card-body">
              <div class="card-top">
                <div>
                  <h3>{product.name}</h3>
                  <span class="product-type">{product.type}</span>
                </div>
                <span class="product-pitch">{product.pitch}</span>
              </div>
              <p>{product.description}</p>
              <div class="product-features">
                {product.features.map(f => <span class="ftag">{f}</span>)}
              </div>
              <a href="/contact" class="product-cta">
                Demander un devis
                <svg width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Cabinets -->
  <section class="section section-alt" id="cabinets">
    <div class="container">
      <div class="category-header">
        <h2>Cabinets LED</h2>
        <p>Systemes modulaires avec processeur LED deporte pour une flexibilite maximale.</p>
      </div>
      <div class="products-grid">
        {cabinets.map(product => (
          <div class="product-card">
            <div class="card-img">
              <img src={product.image} alt={product.name} loading="lazy" />
              {product.tag && <span class="card-tag">{product.tag}</span>}
            </div>
            <div class="card-body">
              <div class="card-top">
                <div>
                  <h3>{product.name}</h3>
                  <span class="product-type">{product.type}</span>
                </div>
                <span class="product-pitch">{product.pitch}</span>
              </div>
              <p>{product.description}</p>
              <div class="product-features">
                {product.features.map(f => <span class="ftag">{f}</span>)}
              </div>
              <a href="/contact" class="product-cta">
                Demander un devis
                <svg width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Specialty -->
  <section class="section" id="outdoor">
    <div class="container">
      <div class="category-header">
        <h2>Solutions speciales</h2>
        <p>Outdoor haute luminosite et formats cinematiques pour des installations uniques.</p>
      </div>
      <div class="products-grid">
        {specialty.map(product => (
          <div class="product-card">
            <div class="card-img">
              <img src={product.image} alt={product.name} loading="lazy" />
              {product.tag && <span class="card-tag">{product.tag}</span>}
            </div>
            <div class="card-body">
              <div class="card-top">
                <div>
                  <h3>{product.name}</h3>
                  <span class="product-type">{product.type}</span>
                </div>
                <span class="product-pitch">{product.pitch}</span>
              </div>
              <p>{product.description}</p>
              <div class="product-features">
                {product.features.map(f => <span class="ftag">{f}</span>)}
              </div>
              <a href="/contact" class="product-cta">
                Demander un devis
                <svg width="14" height="14" viewBox="0 0 16 16" fill="none"><path d="M3 8h10M9 4l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

</Layout>

<style>
  .page-hero {
    padding: 4rem 0 3rem;
    background: var(--color-bg-alt);
    border-bottom: 1px solid var(--color-border);
  }

  .page-hero h1 { margin-bottom: 0.75rem; }

  .page-hero-desc {
    font-size: 1.1rem;
    max-width: 560px;
  }

  .category-header {
    margin-bottom: 2rem;
  }

  .category-header h2 { margin-bottom: 0.5rem; }

  .category-header p {
    font-size: 1rem;
    max-width: 500px;
  }

  .section-alt {
    background: var(--color-bg-alt);
  }

  .products-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .product-card {
    background: #ffffff;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    overflow: hidden;
    transition: border-color var(--transition-base), box-shadow var(--transition-base);
  }

  .product-card:hover {
    border-color: var(--color-primary-light);
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
  }

  .card-img {
    position: relative;
    overflow: hidden;
  }

  .card-img img {
    width: 100%;
    height: 170px;
    object-fit: cover;
    display: block;
    transition: transform 0.4s ease;
  }

  .product-card:hover .card-img img {
    transform: scale(1.03);
  }

  .card-tag {
    position: absolute;
    top: 0.6rem;
    left: 0.6rem;
    font-size: 0.65rem;
    font-weight: 600;
    padding: 0.2rem 0.55rem;
    background: rgba(0, 0, 0, 0.55);
    backdrop-filter: blur(4px);
    border-radius: var(--radius-sm);
    color: #fff;
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .card-body {
    padding: 1.25rem;
  }

  .card-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    margin-bottom: 0.5rem;
  }

  .card-top h3 {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--color-text);
  }

  .product-type {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    font-weight: 500;
  }

  .product-pitch {
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-primary);
    white-space: nowrap;
  }

  .card-body > p {
    font-size: 0.88rem;
    color: var(--color-text-secondary);
    margin-bottom: 0.85rem;
    line-height: 1.55;
  }

  .product-features {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin-bottom: 0.85rem;
  }

  .ftag {
    font-size: 0.68rem;
    font-weight: 500;
    padding: 0.15rem 0.5rem;
    background: var(--color-bg-alt);
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-sm);
    color: var(--color-text-secondary);
  }

  .product-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-primary);
    transition: gap var(--transition-fast);
  }

  .product-cta:hover { gap: 0.55rem; }

  @media (max-width: 1024px) {
    .products-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .products-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
